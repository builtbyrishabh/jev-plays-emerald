# First Gym Implementation Plan

> **For agentic workers:** Use superpowers:subagent-driven-development for independent mechanics/reference/viewer tasks, with root owning progression and integration.

**Goal:** Extend autonomous Emerald play to a verified first badge and present the journey clearly.

**Architecture:** Keep the current Python emulator loop, legal actions, Jev player and bounded Luna coach. Add target-aware progress and reference data; reuse PokéBot controls.

**Tech Stack:** Python 3.13, pinned PokéBot/mGBA, existing TypeScript service, plain HTML/CSS/JS.

**Spec:** `docs/superpowers/specs/2026-09-22-first-gym-design.md`

## Global Constraints

- Preserve current uncommitted work and existing profiles; use fresh run directories.
- No new dependencies or services; no story/party/RNG writes for gameplay evidence.
- Model chooses meaningful actions; deterministic code only executes choices or forced progression.
- Local tests cannot establish autonomous gameplay completion.

## Review Focus

- Loaded badge cannot count as a new victory; test transition and battle identity.
- Rival completion must retain actions in first-gym mode; explicit rival benchmark still stops.
- Milestone coaching must not repeat every frame or erase useful advice on ordinary map changes.
- New menus cannot leave a no-action deadlock; test replacements and dialogue using real routines.
- Viewer must distinguish checkpoint, verified completion and incomplete run.

### Task 1: Progression and planner (root)
- [x] Add failing tests in `tests/test_journey.py` for target selection, milestone stages, badge-win proof and coaching triggers.
- [x] Implement `journey.py`, extend `OpeningFlags`, and connect `opening.py`, `mode.py`, `planner.py` and knowledge stages. Use `JEV_TARGET=first-gym|rival` with first-gym app default and explicit rival benchmark default.
- [x] Run focused tests, then update benchmark target selection and documentation.

### Task 2: Gameplay mechanics (independent worker)
- [x] Read pinned upstream capabilities and add focused tests for first-gym menu handling.
- [x] Implement normal-input healing/dialogue/shop/items/replacement/learning/evolution support in `actions.py`, `state.py` and a new `gameplay.py`; root integrates action enumeration in `opening.py`.
- [x] Return exact state fields and action interfaces; verify with ROM probes where available.

### Task 3: Pokémon reference (independent worker)
- [x] Test species/type-effectiveness/learnset lookup against pinned upstream data.
- [x] Create `pokemon_reference.py`; attach bounded reference via `prompts.py` to player and planner observations without hidden battle data.
- [x] Validate reference and inspect token-size impact.

### Task 4: Viewer (independent worker)
- [x] Update `plugins/jev_emerald.py`, `web/` and viewer tests for first-gym milestones, party HP and checkpoint semantics.
- [x] Consume mode `target`, `checkpoint`, `completed` and `manual_actions` properties, and observation flags `stone_badge`, `petalburg_tutorial`, `devon_goods_saved`.
- [x] Verify wide/narrow rendering using available browser tools.

### Task 5: Integration and evidence (root)
- [x] Run Python suite, TypeScript check and service tests; review independent changes.
- [x] Run fresh bounded hybrid gameplay, diagnose observed blockers and repair within scope.
- [x] Record exact outcome, checks and limits; update README/setup/results and final report.

## Execution record

User authorization: “You make the decision and just give me final output”, followed by “Do it”. Continue directly through implementation and verification. Work stays on the existing feature branch because required uncommitted runtime work is already here; no reset, stash, commit, push or PR is needed.

Stopped live testing at the user’s request. Implementation and control checks are complete; autonomous first-badge completion is unproven. Final verification: 323 Python tests, 14 ROM subtests, TypeScript checking, and 19 service tests. Evidence and unfinished gameplay objective are recorded in `docs/first-gym.md`. No commit, push, or PR was created.
